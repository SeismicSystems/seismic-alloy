//! Example programs for the seismic-alloy SDK.
//!
//! Run them with: `cargo run -p seismic-examples --example <name>`
//!
//! Available examples:
//! - `basic_contract` — Deploy, shielded reads/writes, transparent reads/writes
//! - `eip712` — EIP-712 typed data signing
//! - `security_params` — Custom expiration, nonce, block hash
//! - `event_subscription` — WebSocket event subscription

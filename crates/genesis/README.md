# seismic-alloy-genesis

Seismic genesis file definitions.

The genesis file in an Seismic blockchain is a JSON-formatted file used to
define the initial state of the blockchain at the time of its creation. Seismic allows for private state at genesis.
# seismic-alloy-genesis

Seismic genesis file definitions — an additive companion to `alloy-genesis` that adds
`FlaggedStorage` support for private storage at genesis.

See the [module documentation](src/lib.rs) for details on the relationship to upstream
`alloy-genesis`, what this crate defines vs. reuses, and usage across the Seismic codebase.
